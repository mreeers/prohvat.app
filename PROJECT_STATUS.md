# Статус проекта "Prohvat"

## 🚀 Что уже реализовано

### Backend (ASP.NET Core Web API)
* **Архитектура:** Построена на CQRS с использованием MediatR, Entity Framework Core (PostgreSQL) в качестве ORM.
* **Аутентификация и авторизация:** Реализована на базе JWT-токенов. Настроены кастомные claims (например, `username` и `sub`) для удобной работы на клиенте.
* **Покатушки (Rides):**
  * Создание покатушек с указанием гео-координат.
  * Получение покатушек в заданной области карты (Bounding Box).
  * Возможность присоединиться и покинуть покатушку.
  * Публичный просмотр списка покатушек на карте.
* **Споты (Spots):**
  * Добавление интересных мест на карту.
  * Публичное отображение спотов.
* **Бортжурналы (Vehicles):**
  * Добавление транспортных средств (мотоциклов и др.).
  * Ведение логов обслуживания/поездок с привязкой к ТС.
  * Публичный просмотр профиля ТС и истории логов (без необходимости авторизации).
  * Редактирование и добавление логов доступно только владельцу.
* **Профили пользователей:**
  * Просмотр профиля по уникальному username.
  * Отображение связанной информации (добавленные ТС).
  * Загрузка аватарок и медиафайлов через **MinIO (S3-совместимое хранилище)**.
* **Социальное взаимодействие (Social Core):**
  * Комментарии к бортжурналам, покатушкам и спотам.
  * Система лайков (оценок).
  * Оставление отзывов с рейтингом.
* **Real-time (SignalR):**
  * Живой приватный чат внутри покатушек (только для участников).
  * Мгновенные In-App уведомления о новых лайках и комментариях.

* **Социальная лента активности (Feed - ✅ РЕАЛИЗОВАНО):**
  * CQRS-запрос `GetFeedQuery` с фильтрацией «Все записи» / «Новости друзей» и поддержкой сезонности (Лето/Зима).
  * Агрегация бортжурналов техники (`VehicleLog` из MongoDB) и покатушек (`Ride` из PostgreSQL).
  * Интеграция с базой тюнинга (`PartReview`): подтягивание установленных деталей, артикулов и ссылок на магазины.
  * Полноценная статистика лайков и комментариев с признаком `isLikedByCurrentUser`.
  * Эндпоинт `GET /api/feed` (`FeedController`).
* **Сидер реалистичных тестовых данных (Seed Data - ✅ РЕАЛИЗОВАНО):**
  * Автоматическое создание 4 райдеров (Алексей Гончаров `rider_alex`, Дарья Ветрова `daria_drift`, Иван Черепанов `ivan_enduro`, Сергей Морозов `sergey_snow`).
  * Полные профили с аватарами, геолокацией и описанием.
  * Гаражи с техникой: KTM 300 EXC, ВАЗ-2105 «Боевая Классика» (дрифт), ВАЗ-2107 «Дрифт-пушка», Kawasaki Ninja 650, Husqvarna TE 300, BRP Ski-Doo Summit 850 (снегоход).
  * Записи бортжурналов с фотоотчетами, видео и метриками.
  * Взаимная дружба, активные покатушки, взаимные лайки и комментарии.
  * Тестовый вход: `alex@prohvat.app` (пароль `0102`), остальные `password123`.
  * Решена проблема сериализации `Guid` в MongoDB 4.4 (`GuidSerializer.Standard`).
  * Обеспечена автоматическая дозагрузка бортжурналов в MongoDB при перезапуске сервера без сброса PostgreSQL.

* **GPX-треки и детальный просмотр маршрутов покатушек (Rides & GPX - ✅ РЕАЛИЗОВАНО):**
  * Эндпоинт `GET /api/rides/{id}`: детальные данные покатушки, точки трека, организатор, участники и статус заезда.
  * Эндпоинт `POST /api/rides/{id}/gpx`: загрузка `.gpx` файлов в MinIO S3 и парсинг точек.
  * Поддержка прикрепления GPX-трека сразу при создании покатушки (`POST /api/rides`).
  * Утилита `GpxHelper`: парсинг XML-точек трека и расчет точной дистанции по формуле Haversine.
  * Модальное окно `RideDetailModal.vue`: интерактивная карта с неоновой линией трека, маркерами «Старт» и «Финиш», бейджем дистанции, списком участников и кнопкой загрузки трека организатором.
  * Драг-энд-дроп загрузка GPX в `CreateRideView.vue` с мгновенным предпросмотром нитки маршрута на карте.
  * Интерактивная кнопка «Маршрут и GPX-трек» на карточках покатушек в социальной ленте (`FeedView.vue`).

* **Динамический Тюнинг-Конфигуратор гаража (Tuning Configurator - ✅ РЕАЛИЗОВАНО):**
  * Компонент `TuningConfigEditor.vue`, динамически адаптирующийся под категорию техники:
    * *Боевая Классика / Дрифт (ВАЗ):* выворот передней подвески (Красноярский, Турботема, We Ride), блокировка редуктора (Заварка 4.1, 4.3, 3.9), гидроручник, двигатель (карб спорт, 16v шеснарь атмо/турбо), каркас и распорки.
    * *Эндуро / Кросс:* подвеска (WP XACT Pro, KYB Factory, ревавлинг), защита (комплект Арма: радиаторы+картер), шины и муссы (Mitas 754 + Tubliss, Michelin + Mousse), выхлоп, впуск.
    * *Снегоходы:* трак и зацеп (3.0" PowderMax, 2.6"), лыжи (Blade DS+), бамперы Voevoda, проставка руля.
  * Интеграция конфигуратора в формы добавления (`NewVehicleView.vue`) и детального просмотра ТС (`VehicleDetailView.vue`).
  * Отображение спек-листа на карточках техники в гараже (`GarageView.vue`).
  * Реалистичные спеки тюнинга для всех демо-машин и мотоциклов.

* **Модуль Геймификации и Достижений (Achievements - ✅ РЕАЛИЗОВАНО):**
  * Сущности `Achievement` и `UserAchievement` в Domain Core и PostgreSQL.
  * Эндпоинт `GET /api/achievements/user/{username}` (`AchievementsController`).
  * 6 аутентичных наград райдеров: «Железная задница» (рейд >150 км), «Утопленник» (затопление/брод), «Первый лед» (ледовый дрифт), «Покоритель вершин» (хард-эндуро скалы), «Царь гаража» (2+ ТС со спеками), «Лидер стаи» (организация покатушки).
  * Вкладка «Достижения» в профиле райдера (`ProfileView.vue`): прогресс-бар наград и очков XP, сетка золотых бейджей с описаниями и датами получения.

### Frontend (Vue 3 + Vite)
* **Стек:** Vue 3 (Composition API), Pinia (стейт-менеджмент), Vue Router.
* **Карта:** Интеграция с Яндекс.Картами для отображения интерактивной карты, покатушек, спотов и GPX-треков.
* **UX/UI (Редизайн в стиле VK 2015):** 
  * Глобальный редизайн с сохранением темной премиальной темы.
  * Фиксированная верхняя шапка и классическое левое меню (Моя страница, Новости, Друзья, Карта, Споты, Гараж, Настройки).
  * Разделение контента на тематические View для упрощения навигации.
* **Социальная лента (FeedView.vue - ✅ РЕАЛИЗОВАНО):**
  * Вкладки «Все записи» и «Новости друзей».
  * Сезонные фильтры: «Все сезоны», «☀️ Лето (Эндуро)», «❄️ Зима (Дрифт / Снегоходы)».
  * Быстрый виджет публикации новой записи в бортжурнал техники («Что у вас нового?»).
  * Карточки постов: автор с аватаром и временем, тег техники с пробегом/моточасами, галерея картинок с лайтбоксом, видео-плееры, блок установленного тюнинга.
  * Интерактивные лайки (сердечки) с мгновенным откликом.
  * Разворачиваемые комментарии под каждым постом с возможностью отправки новых комментариев без перезагрузки.
* **Социальные функции (Фронтенд):**
  * Управление друзьями (заявки, удаление) прямо из профиля.
  * Выделенная страница "Мои друзья" (разделение на активных друзей и заявки).

---

## 🛠 Предложения по оптимизации для Production

Для подготовки проекта к высоким нагрузкам и развертыванию в Production, рекомендуется внедрить следующие технологии и подходы:

### 1. Кэширование (Redis)
* **Зачем:** Для снижения нагрузки на базу данных PostgreSQL и ускорения отдачи данных.
* **Что кэшировать:** 
  * Справочники (список регионов, категорий транспорта).
  * Активные покатушки в популярных регионах.
  * Сессии пользователей и токены (если используется stateful подход или для отзывов токенов/refresh tokens).
  * Данные профилей, которые редко меняются.

### 2. Асинхронная обработка и очереди (RabbitMQ / Kafka)
* **Зачем:** Для развязки компонентов системы, повышения отказоустойчивости и асинхронного выполнения тяжелых задач.
* **Где применить:**
  * Отправка Email / Push-уведомлений (например, "В вашем регионе создана новая покатушка").
  * Асинхронная обработка загружаемых изображений (ресайз, компрессия, создание превью для бортжурналов).
  * Сбор и агрегация аналитики (кто куда кликал, какие споты самые популярные).

### 3. Real-time взаимодействие (SignalR / WebSockets) - **✅ ЧАСТИЧНО РЕАЛИЗОВАНО (Чат + Уведомления)**
* **Что осталось:**
  * Трансляция геолокации участников в реальном времени (Live Tracking) на карте во время активной покатушки.

### 4. Хранение медиафайлов (MinIO / AWS S3) - **✅ РЕАЛИЗОВАНО**
* Интегрирован `MinioFileService`. Все аватарки и фото теперь надежно сохраняются в отдельном бакете S3, что разгружает основной сервер.

### 5. Поиск и фильтрация (Elasticsearch)
* **Зачем:** Если появится сложный поиск по тексту (поиск пользователей, полнотекстовый поиск по бортжурналам, комментариям, тегам).
* **Как реализовать:** Синхронизация данных из PostgreSQL в Elasticsearch через RabbitMQ или напрямую (Debezium/CDC).

### 6. Инфраструктура и DevOps (Docker, CI/CD) - **✅ РАЗВЕРНУТО НА ТЕСТОВОМ СЕРВЕРЕ**
* **Тестовый сервер (192.168.1.197):**
  * Настроен SSH-доступ по ed25519-ключу (`alex@192.168.1.197`).
  * Настроен автоматический CI/CD деплой: команда `git push vm main` отправляет код в `prohvat.git` на сервере, где Git hook `post-receive` мгновенно собирает и перезапускает контейнеры.
  * Скрипт `deploy.sh` обновлен и поддерживает как автоматический деплой, так и ручной запуск на сервере.
  * Все 7 сервисов (Postgres+PostGIS, Redis, RabbitMQ, MongoDB 4.4, MinIO, Backend, Frontend) успешно работают в Docker.
  * Reverse Proxy (Nginx) маршрутизирует веб-интерфейс (порт 80/8080), API (`/api/`), SignalR (`/hubs/`) и хранилище S3 (`/s3/`).
* **UI/UX и TypeScript фиксы:** 
  * Исправлен полноэкранный режим карты при редизайне под VK-стиль.
  * Добавлен корневой `tsconfig.json` со ссылками на проекты frontend, исправлен `frontend/tsconfig.node.json` (устранены ошибки обнаружения типов Node в IDE).

### 7. Мониторинг и логирование (Prometheus + Grafana, ELK / Seq)
* Настроить сбор метрик (.NET EventCounters, Prometheus endpoint) для отслеживания нагрузки (CPU, Memory, GC) и RPS.
* Централизованный сбор логов (например, с использованием Serilog + Seq или Elastic Stack), чтобы быстро находить ошибки в проде.

---

## 📝 Что в планах на реализацию (Roadmap)

### Киллер-фичи для Мобильного приложения и Telegram Mini App:
1. **Система Экстренного Оповещения (Кнопка SOS) — ТЗ п. 3.7 и 4.1:**
   * Выделенная крупная кнопка быстрого вызова помощи в мобильном приложении и Telegram Mini App (сценарии оффроуда, застревания, травм).
   * Рассылка экстренных PUSH и Telegram-уведомлений райдерам в радиусе 15 км.
   * Отображение пульсирующей точки бедствия на мобильной карте.
   * Офлайн-генератор коротких SMS: `SOS_[UserId]_[Lat]_[Lng]_[Код_Проблемы]` для отправки по спутниковой/сотовой связи вне интернета.
2. **Telegram Mini App & Бот интеграция:**
   * Авторизация через Telegram Login Widget / WebApp initData.
   * Быстрый доступ к карте покатушек и SOS прямо из мессенджера.
   * Уведомления в Telegram-чат о сборах на покатушки, новых комментариях и друзьях.
3. **Live Tracking (Режим реального времени в движении):**
   * Фоновая передача GPS-координат участников во время активной покатушки в мобильном приложении.

